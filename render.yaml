services:
  - type: web
    name: gigamind-proxy
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true
    envVars:
      - key: GIGAMIND_KEY
        sync: false
      - key: PROXY_KEY
        sync: false
